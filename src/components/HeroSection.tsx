
import React from 'react';

const HeroSection = () => {
  return (
    <section className="pb-2 animate-fade-in">
      {/* Empty hero section as requested */}
    </section>
  );
};

export default HeroSection;
